enum Categories{
    FREE,
    PERSONAL,
    PREMIUM
}

enum Topup{
  FOUR_DEVICE,
  TEN_DEVICE
}

// Ideally we should keep the catgeories and prices as external files beacuse we might want to update them later and doing so we should not need to change our code nor should it break